CREATE TABLE `test1`.`department` (
  `sno` INT NOT NULL,
  `departmentId` INT NOT NULL,
  `departmentName` VARCHAR(45) NOT NULL,
  PRIMARY KEY (`departmentId`),
  UNIQUE INDEX `departmentId_UNIQUE` (`departmentId` ASC) VISIBLE);
  
 CREATE TABLE test1.FACULTY (SNO INT,DEPARTMENTID INT,FACULTYID INT,FACULTYNAME VARCHAR(2),PRIMARY KEY (SNO),
FOREIGN KEY(DEPARTMENTID) REFERENCES DEPARTMENT(DEPARTMENTID));

CREATE TABLE test1.STUDENT(SNO INT,DEPARTMENTID INT,STUDENTID INT,STUDENTNAME VARCHAR(2),
PRIMARY KEY(SNO),FOREIGN KEY(DEPARTMENTID) REFERENCES DEPARTMENT(DEPARTMENTID));

INSERT INTO TEST1.DEPARTMENT VALUES(1,1,'CSE');

SELECT * FROM TEST1.DEPARTMENT;

INSERT INTO TEST1.FACULTY VALUES(1,1,1,'AB');

SELECT * FROM TEST1.FACULTY;

INSERT INTO TEST1.STUDENT VALUES(1,1,1,'XY');

SELECT * FROM TEST1.STUDENT;

Foreign key error:
Error Code: 1452. Cannot add or update a child row: a foreign key constraint fails (`test1`.`faculty`, CONSTRAINT `faculty_ibfk_1` FOREIGN KEY (`DEPARTMENTID`) REFERENCES
 `department` (`departmentId`))
